\* Diagnostic: fix only the target-timestamp side (fold start_time into
\* target_ts) and leave the landing-position recovery unfixed. Shows that
\* the "obvious" half-fix is still wrong -- both sides of the round trip
\* have to agree on start_time, or the seek lands in the right place but the
\* code mis-identifies which frame it landed on.

SPECIFICATION Spec

CONSTANTS
    NumFrames     = 12
    Keyframes     = {0, 6}
    SeekThreshold = 3
    PtsAvailable  = TRUE
    TicksPerFrame        = 4
    StartTimeTicks        = 5
    FixTargetStartTime    = TRUE
    FixRecoveryStartTime  = FALSE

INVARIANTS
    TypeOK
    PositionAccurate
