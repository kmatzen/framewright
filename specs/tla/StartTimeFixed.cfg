\* The actual fix (src/VideoReader.cpp:816-825,869-872): both the target
\* timestamp and the landing-position recovery fold in the stream's
\* start_time. Same non-zero, non-frame-aligned StartTimeTicks as
\* StartTimeBug.cfg -- only the two toggles differ.

SPECIFICATION Spec

CONSTANTS
    NumFrames     = 12
    Keyframes     = {0, 6}
    SeekThreshold = 3
    PtsAvailable  = TRUE
    TicksPerFrame        = 4
    StartTimeTicks        = 5
    FixTargetStartTime    = TRUE
    FixRecoveryStartTime  = TRUE

INVARIANTS
    TypeOK
    PositionAccurate
