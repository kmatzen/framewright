\* Diagnostic: fix only the landing-position recovery (subtract start_time
\* back out of the decoded frame's pts) and leave target_ts unfixed. Shows
\* the other half-fix is also wrong on its own -- the seek lands in the
\* wrong place, and now the recovery math confidently reports the wrong
\* frame index for it instead of at least being consistently offset.

SPECIFICATION Spec

CONSTANTS
    NumFrames     = 12
    Keyframes     = {0, 6}
    SeekThreshold = 3
    PtsAvailable  = TRUE
    TicksPerFrame        = 4
    StartTimeTicks        = 5
    FixTargetStartTime    = FALSE
    FixRecoveryStartTime  = TRUE

INVARIANTS
    TypeOK
    PositionAccurate
