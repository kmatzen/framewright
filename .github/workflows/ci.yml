name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            name: Linux
          - os: macos-latest
            name: macOS

    runs-on: ${{ matrix.os }}
    name: ${{ matrix.name }}

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libopencv-dev \
            libavformat-dev libavcodec-dev libswscale-dev libavutil-dev \
            pkg-config \
            ffmpeg \
            libx264-dev libx265-dev

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install opencv ffmpeg pkg-config x264 x265

      - name: Configure
        run: |
          cmake -B build \
            -DCVFFMPEG_BUILD_TESTS=ON \
            -DCVFFMPEG_BUILD_EXAMPLES=ON \
            -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build -j$(nproc 2>/dev/null || sysctl -n hw.ncpu)

      - name: Test
        run: ctest --test-dir build --output-on-failure
